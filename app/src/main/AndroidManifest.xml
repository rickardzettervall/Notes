<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tech.zettervall.mNotes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_agenda"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_agenda"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name="tech.zettervall.notes.TrashActivity"
            android:label="@string/action_trash" />
        <activity
            android:name="tech.zettervall.notes.AboutActivity"
            android:label="@string/action_about" />
        <activity
            android:name="tech.zettervall.notes.SettingsActivity"
            android:label="@string/action_settings" />
        <activity android:name="tech.zettervall.notes.NoteActivity"
            android:label="@string/note"/>
        <activity
            android:name="tech.zettervall.notes.MainActivity"
            android:label="@string/action_all_notes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>